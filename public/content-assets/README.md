# Content assets

Drop captured screenshots here. Register each file in `src/content/assets.ts`
(`publishedAssets[AssetRef.id] = { url, width, height }`).

- URL path: `/content-assets/<filename>`
- Alt text lives on the `AssetRef` in content, written at capture time
- Do not add lorem, AI product shots, or files that are not in the gallery cut
